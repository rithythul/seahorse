services:
  seahorse:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    env_file: ../.env
    volumes:
      - seahorse_data:/data
    restart: unless-stopped

volumes:
  seahorse_data:
